# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Example' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Example
  pod 'SwiftLint'

end

target 'Turing' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Turing
  pod 'SwiftLint'

  target 'TuringTests' do
    # Pods for testing
    pod 'SwiftLint'
  end

end
